<view class="label-value flex">
  <text class="Label" decode="{{true}}">{{label}}：</text>
  <input type="{{type}}" bindblur="changeStatus" id="{{name}}" class="label-value-input" disabled="{{ifInput}}"/>
</view>